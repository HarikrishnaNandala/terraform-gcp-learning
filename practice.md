Practicing Git Commands
Git Init -- Initialization of the GIT. This command will generate .git hidden folder inside the folder (this converts normal folder into git). This command will use at the begining
Git config  --global "user.name"
Git config --global "yourname@example.com"
git config --global init.defaultBranch main
git config --global core.editor "code --wait"
git config --list
ssh-keygen -t ed25519 -C "you@example.com"
